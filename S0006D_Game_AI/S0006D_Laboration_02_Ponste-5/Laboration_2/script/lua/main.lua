

--aStar = require "script/lua/pathfinding/pathfinding_a"
--local star = aStar:init()
--Entity_Manager = Entity_Manager

local loadedMap = false



function Update()


    -- while true do
    --     if stingray.Keyboard.pressed(keyQ) then
    --         break
    --     end
    --     worker:Update()

    -- end
    -- return


        -- local startTime = os.clock()
        -- star:AStar(startNode,goalNode, planeArr, width)
        -- local finishTime = os.clock()
        -- local totalTime = finishTime - startTime
        -- print("Finished in: "..totalTime)
        
        -- for n, no in ipairs(star.bestPath) do
        --     local Pos = stingray.Vector3(no.x,no.y,0)
        --     table.insert(pathArr,stingray.World.spawn_unit(world, "content/models/character/character",Pos))
        -- end
    -- while true do
    --     if #worker.currentPath ~= 0 then
    --         worker:Update()
    --     end

    -- end
end